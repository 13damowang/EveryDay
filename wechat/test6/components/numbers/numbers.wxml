<!--components/numbers/numbers.wxml-->
<view>
  {{numA}} + {{numB}} = {{sum}}
</view>
<van-button type="primary" bindtap="numBadd" data-step="{{1}}">numB +1</van-button>
<van-button type="warning" bindtap="numBadd" data-step="{{-1}}">numB -1</van-button>

